#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main(){
    int x = 10.2;
    x = sqrt(x);
    x = pow(x,2);
    x = log(x);
    x = sin(x);
    x = cos(x);
    x = tan(x);  
    x = abs(x);
    x = round(x);

    printf("%d",x);

}